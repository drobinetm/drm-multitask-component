# @drm/multitabs-angular

## 0.1.2

### Patch Changes

- b48a1be: Prepare the Angular package release flow for npm publication and build the
  package during `prepack` so published artifacts always include `dist`.

## 0.1.1

### Patch Changes

- Fix Angular package build and workspace tooling compatibility by aligning
  TypeScript requirements, adding `tslib`, and updating package scripts used in
  the monorepo.

<section class="section" style="padding-top: 4.6rem; padding-bottom: 2.4rem; overflow: clip;">
  <div class="shell hero-intro" data-aos="fade-up" data-aos-once="true">
    <div class="eyebrow" style="justify-content:center;">Browser-tab navigation, reimagined</div>
    <h1 class="headline" style="max-width: 11ch; margin:0 auto;">Multi-tabs that feel native inside your app.</h1>
    <p class="subhead" style="margin:1.3rem auto 0; max-width: 52rem;">
      <code>@drm/multitabs</code> brings sticky browser-style tabs, drag and drop,
      tab persistence, route-aware navigation, and reload hooks to Vue, React,
      and Angular applications without forcing a design system.
    </p>

    <div class="hero-actions">
      <a class="button button--primary" href="/docs/getting-started">Read the docs</a>
      <a class="button button--ghost" href="/support">Support the project</a>
    </div>

    <div class="hero-tags">
      <span class="hero-badge glass">
        <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 3 4 7v5c0 5 3.4 8.5 8 9 4.6-.5 8-4 8-9V7l-8-4Z"></path><path d="M9 12h6"></path><path d="M12 9v6"></path></svg>
        <span>Vue Router</span>
      </span>
      <span class="hero-badge glass">
        <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="2.4"></circle><path d="M12 4c3.6 0 6.8 2 8.4 5-1.6 3-4.8 5-8.4 5S5.2 12 3.6 9C5.2 6 8.4 4 12 4Z"></path><path d="M12 14c-3.6 0-6.8 2-8.4 5 1.6-3 4.8-5 8.4-5s6.8 2 8.4 5c-1.6-3-4.8-5-8.4-5Z"></path></svg>
        <span>React Router</span>
      </span>
      <span class="hero-badge glass">
        <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 3 5 6l1.2 9.3L12 21l5.8-5.7L19 6l-7-3Z"></path><path d="M12 7v8"></path><path d="M9 11.5h6"></path></svg>
        <span>Angular Router</span>
      </span>
      <span class="hero-badge glass">
        <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 7h16"></path><path d="M4 12h10"></path><path d="M4 17h7"></path><circle cx="17" cy="12" r="2"></circle><circle cx="14" cy="17" r="2"></circle></svg>
        <span>Theme-ready CSS vars</span>
      </span>
    </div>
  </div>

  <style>
    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.55rem;
      padding: 0.68rem 0.95rem;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.05);
      color: rgba(233, 240, 255, 0.9);
      font-weight: 600;
      line-height: 1;
    }

    .hero-badge svg {
      width: 0.95rem;
      height: 0.95rem;
      flex-shrink: 0;
      fill: none;
      stroke: #73beff;
      stroke-width: 1.8;
      stroke-linecap: round;
      stroke-linejoin: round;
      opacity: 0.95;
    }
  </style>

  <div class="shell" style="margin-top:1.7rem;" data-aos="fade-up" data-aos-delay="100" data-aos-once="true">
    <div class="glass" style="position:relative; overflow:hidden; padding:1rem; border-radius:36px; background:linear-gradient(180deg, rgba(10,18,34,0.72), rgba(6,12,24,0.94));">
      <div style="position:absolute; inset:0; background:radial-gradient(circle at 70% 24%, rgba(41,106,255,0.24), transparent 18%), radial-gradient(circle at 84% 46%, rgba(80,212,255,0.14), transparent 16%), linear-gradient(180deg, rgba(255,255,255,0.015), transparent 34%); pointer-events:none;"></div>

      <div style="position:relative; border-radius:30px; overflow:hidden; background:linear-gradient(180deg, #091323 0%, #050b16 100%); border:1px solid rgba(255,255,255,0.05);">
        <div class="hero-surface__body" style="padding:1.5rem; display:grid; gap:1.4rem;">
          <div class="hero-copy" style="display:grid; align-content:center; gap:1rem; text-align:left;">
            <div class="eyebrow" style="margin-bottom:0;">Live composition</div>
            <h2 style="margin:0; font-size:clamp(2.4rem, 4vw, 4rem); line-height:0.92; letter-spacing:-0.05em;">Workspace multitabs for dense internal apps.</h2>
            <p class="panel-copy" style="max-width:42rem;">See a dashboard-style multitabs composition with a header, product-like tabs, circular icons, and close controls that make the component read like a real internal app shell.</p>
            <div class="hero-copy__actions" style="margin-top:0.3rem;">
              <a class="button button--primary" href="/docs/getting-started">Install now</a>
              <a class="button button--ghost" href="/docs/vue">Explore APIs</a>
            </div>
          </div>

          <div class="live-composition-demo" aria-label="Animated dashboard multitabs showcase with header and product-like tabs">
            <div class="live-composition-demo__bar">
              <span class="live-composition-demo__dot"></span>
              <span class="live-composition-demo__dot"></span>
              <span class="live-composition-demo__dot"></span>
              <span class="live-composition-demo__title">@drm/multitabs live composition</span>
            </div>

            <div class="live-composition-demo__shell">
              <div class="dashboard-header" aria-hidden="true">
                <div class="dashboard-header__brand">
                  <div>
                    <strong>Dashboard workspace</strong>
                    <span>Header first, multitabs below</span>
                  </div>
                </div>

                <div class="dashboard-header__meta">
                  <span class="dashboard-chip dashboard-chip--vue">Vue</span>
                  <span class="dashboard-chip dashboard-chip--react">React</span>
                  <span class="dashboard-chip dashboard-chip--angular">Angular</span>
                </div>
              </div>

              <div class="tab-strip" aria-hidden="true">
                <div class="tab-strip__track">
                  <span class="tab-pill tab-pill--active">
                    <span class="tab-pill__icon" aria-hidden="true"></span>
                    <span class="tab-pill__label">Home</span>
                    <span class="tab-pill__close">x</span>
                  </span>
                  <span class="tab-pill">
                    <span class="tab-pill__icon" aria-hidden="true"></span>
                    <span class="tab-pill__label">Settings</span>
                    <span class="tab-pill__close">x</span>
                  </span>
                  <span class="tab-pill">
                    <span class="tab-pill__icon" aria-hidden="true"></span>
                    <span class="tab-pill__label">About</span>
                    <span class="tab-pill__close">x</span>
                  </span>
                  <span class="tab-pill">
                    <span class="tab-pill__icon" aria-hidden="true"></span>
                    <span class="tab-pill__label">Clients</span>
                    <span class="tab-pill__close">x</span>
                  </span>
                </div>
              </div>

              <div class="workspace-stage" aria-hidden="true">
                <div class="workspace-stage__card workspace-stage__card--primary">
                  <div class="workspace-stage__eyebrow">Dashboard header</div>
                  <strong>Keep long-lived app work visible above the multitabs strip</strong>
                  <p>Give internal tools a persistent header, then let multitabs carry navigation state across Home, Settings, About, and Clients.</p>
                </div>
                <div class="workspace-stage__card workspace-stage__card--secondary">
                  <span>Close buttons</span>
                  <span>Circular icons</span>
                  <span>Dashboard rhythm</span>
                </div>
                <div class="workspace-stage__panel workspace-stage__panel--vue">
                  <div class="workspace-stage__panel-label">Clients overview</div>
                  <div class="workspace-stage__panel-line"></div>
                  <div class="workspace-stage__panel-line workspace-stage__panel-line--short"></div>
                </div>
                <div class="workspace-stage__panel workspace-stage__panel--react">
                  <div class="workspace-stage__panel-label">Settings state</div>
                  <div class="workspace-stage__panel-line"></div>
                  <div class="workspace-stage__panel-line workspace-stage__panel-line--short"></div>
                </div>
                <div class="workspace-stage__panel workspace-stage__panel--angular">
                  <div class="workspace-stage__panel-label">About context</div>
                  <div class="workspace-stage__panel-line"></div>
                  <div class="workspace-stage__panel-line workspace-stage__panel-line--short"></div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    .live-composition-demo {
      position: relative;
      overflow: hidden;
      border-radius: 28px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      background:
        radial-gradient(circle at top right, rgba(86, 178, 255, 0.2), transparent 30%),
        linear-gradient(180deg, rgba(9, 17, 31, 0.96), rgba(5, 10, 20, 0.98));
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.05);
    }

    .live-composition-demo__bar {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.8rem 1rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.06);
      background: rgba(255, 255, 255, 0.03);
    }

    .live-composition-demo__dot {
      width: 0.62rem;
      height: 0.62rem;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.18);
    }

    .live-composition-demo__title {
      margin-left: 0.4rem;
      font-size: 0.82rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: rgba(214, 228, 255, 0.7);
    }

    .live-composition-demo__shell {
      display: grid;
      gap: 1.1rem;
      padding: 1.15rem;
    }

    .dashboard-header {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      gap: 0.9rem;
      padding: 0.95rem 1rem;
      border-radius: 20px;
      border: 1px solid rgba(255, 255, 255, 0.06);
      background: linear-gradient(180deg, rgba(18, 31, 52, 0.96), rgba(9, 17, 31, 0.94));
    }

    .dashboard-header__brand {
      display: flex;
      align-items: center;
      gap: 0.85rem;
      min-width: 0;
    }

    .dashboard-header__brand strong,
    .dashboard-header__brand span {
      display: block;
      min-width: 0;
    }

    .dashboard-header__brand strong {
      color: #f3f9ff;
      font-size: 1rem;
    }

    .dashboard-header__brand span {
      color: rgba(214, 228, 255, 0.7);
      font-size: 0.82rem;
      margin-top: 0.15rem;
    }

    .dashboard-header__meta {
      display: flex;
      flex-wrap: wrap;
      gap: 0.55rem;
    }

    .dashboard-chip {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.45rem 0.72rem;
      border-radius: 999px;
      border: 1px solid currentColor;
      background: rgba(255, 255, 255, 0.03);
      font-size: 0.76rem;
      font-weight: 700;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }

    .dashboard-chip--vue {
      color: #63d8a3;
    }

    .dashboard-chip--react {
      color: #7ad9ff;
    }

    .dashboard-chip--angular {
      color: #ff6b92;
    }

    .tab-strip {
      overflow: hidden;
      border-radius: 18px;
      border: 1px solid rgba(255, 255, 255, 0.06);
      background: rgba(7, 14, 26, 0.86);
    }

    .tab-strip__track {
      display: flex;
      gap: 0.7rem;
      width: max-content;
      padding: 0.8rem;
      animation: tab-marquee 12s ease-in-out infinite alternate;
    }

    .tab-pill {
      display: inline-flex;
      align-items: center;
      gap: 0.6rem;
      padding: 0.72rem 0.85rem;
      border-radius: 16px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      background: linear-gradient(180deg, rgba(20, 33, 54, 0.92), rgba(10, 18, 33, 0.98));
      color: rgba(226, 236, 255, 0.82);
      white-space: nowrap;
      font-weight: 600;
      box-shadow: inset 0 -1px 0 rgba(255, 255, 255, 0.04);
    }

    .tab-pill__icon,
    .tab-pill__close {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      flex: 0 0 auto;
      width: 1.45rem;
      height: 1.45rem;
      border-radius: 999px;
      font-size: 0.72rem;
      line-height: 1;
      font-weight: 700;
    }

    .tab-pill__icon {
      width: 0.42rem;
      height: 0.42rem;
      background: #8cc6ff;
      border: 0;
      box-shadow: 0 0 0 0.16rem rgba(140, 198, 255, 0.12);
    }

    .tab-pill__label {
      display: inline-flex;
      align-items: center;
    }

    .tab-pill__close {
      background: rgba(255, 255, 255, 0.05);
      color: rgba(233, 240, 255, 0.7);
      border: 1px solid rgba(255, 255, 255, 0.08);
      text-transform: lowercase;
    }

    .tab-pill--active {
      color: #081120;
      background: linear-gradient(135deg, #8cc6ff, #59f2ff);
      border-color: transparent;
      box-shadow: 0 12px 28px rgba(60, 170, 255, 0.22);
      animation: tab-pulse 4.6s ease-in-out infinite;
    }

    .tab-pill--active .tab-pill__icon,
    .tab-pill--active .tab-pill__close {
      color: #06111f;
      background: rgba(255, 255, 255, 0.34);
      border-color: rgba(255, 255, 255, 0.18);
    }

    .tab-pill--active .tab-pill__icon {
      background: #06111f;
      box-shadow: 0 0 0 0.16rem rgba(6, 17, 31, 0.12);
    }

    .workspace-stage {
      position: relative;
      min-height: 22rem;
      border-radius: 22px;
      overflow: hidden;
      border: 1px solid rgba(255, 255, 255, 0.06);
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.04), transparent 32%),
        linear-gradient(135deg, rgba(7, 17, 31, 0.96), rgba(9, 18, 36, 0.9));
    }

    .workspace-stage::before {
      content: "";
      position: absolute;
      inset: 0;
      background:
        linear-gradient(rgba(255, 255, 255, 0.035) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
      background-size: 100% 3.4rem, 3.4rem 100%;
      mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.65), transparent 90%);
      pointer-events: none;
    }

    .workspace-stage__card,
    .workspace-stage__panel {
      position: absolute;
      border-radius: 20px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      background: rgba(10, 20, 36, 0.86);
      box-shadow: 0 18px 40px rgba(2, 8, 18, 0.4);
      backdrop-filter: blur(14px);
    }

    .workspace-stage__card--primary {
      top: 1.3rem;
      left: 1.3rem;
      width: min(100% - 2.6rem, 31rem);
      padding: 1.2rem 1.2rem 1.1rem;
      display: grid;
      gap: 0.45rem;
      animation: float-card 6.2s ease-in-out infinite;
    }

    .workspace-stage__eyebrow {
      font-size: 0.76rem;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: rgba(136, 205, 255, 0.78);
    }

    .workspace-stage__card--primary strong {
      font-size: clamp(1.2rem, 2vw, 1.55rem);
      color: #f7fbff;
      line-height: 1.1;
    }

    .workspace-stage__card--primary p {
      margin: 0;
      color: rgba(214, 225, 245, 0.74);
      max-width: 38ch;
    }

    .workspace-stage__card--secondary {
      right: 1.3rem;
      bottom: 1.3rem;
      padding: 0.9rem;
      display: flex;
      flex-wrap: wrap;
      gap: 0.55rem;
      max-width: 19rem;
      animation: float-card 6.2s ease-in-out infinite reverse;
    }

    .workspace-stage__card--secondary span {
      padding: 0.5rem 0.7rem;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.05);
      color: rgba(232, 240, 255, 0.86);
      font-size: 0.8rem;
      font-weight: 600;
    }

    .workspace-stage__panel {
      width: 10.6rem;
      padding: 0.85rem;
      display: grid;
      gap: 0.55rem;
      animation: panel-cycle 9s ease-in-out infinite;
    }

    .workspace-stage__panel--vue {
      top: 8.2rem;
      right: 2.6rem;
      border-color: rgba(99, 216, 163, 0.38);
      animation-delay: 0s;
    }

    .workspace-stage__panel--react {
      top: 11rem;
      right: 8.6rem;
      border-color: rgba(122, 217, 255, 0.36);
      animation-delay: -3s;
    }

    .workspace-stage__panel--angular {
      top: 13.8rem;
      right: 4.4rem;
      border-color: rgba(255, 107, 146, 0.36);
      animation-delay: -6s;
    }

    .workspace-stage__panel-label {
      font-size: 0.8rem;
      font-weight: 700;
      color: rgba(244, 248, 255, 0.9);
    }

    .workspace-stage__panel-line {
      height: 0.52rem;
      border-radius: 999px;
      background: linear-gradient(90deg, rgba(135, 195, 255, 0.75), rgba(255, 255, 255, 0.12));
    }

    .workspace-stage__panel-line--short {
      width: 68%;
    }

    @keyframes tab-marquee {
      from {
        transform: translateX(0);
      }

      to {
        transform: translateX(-8rem);
      }
    }

    @keyframes tab-pulse {
      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-2px);
      }
    }

    @keyframes float-card {
      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-8px);
      }
    }

    @keyframes panel-cycle {
      0%,
      20%,
      100% {
        opacity: 0.42;
        transform: translateY(0) scale(0.96);
      }

      10% {
        opacity: 1;
        transform: translateY(-4px) scale(1);
      }
    }

    @media (max-width: 900px) {
      .dashboard-header__meta {
        width: 100%;
      }

      .dashboard-chip {
        flex: 1 1 30%;
      }

      .workspace-stage {
        min-height: 27rem;
      }

      .workspace-stage__card--primary {
        width: calc(100% - 2rem);
        left: 1rem;
        top: 1rem;
      }

      .workspace-stage__panel--vue {
        top: 10rem;
        right: 1rem;
      }

      .workspace-stage__panel--react {
        top: 13.1rem;
        right: 4.8rem;
      }

      .workspace-stage__panel--angular {
        top: 16.2rem;
        right: 1.6rem;
      }

      .workspace-stage__card--secondary {
        left: 1rem;
        right: 1rem;
        bottom: 1rem;
        max-width: none;
      }
    }

    @media (max-width: 640px) {
      .dashboard-header {
        padding: 0.85rem;
      }

      .dashboard-header__brand {
        width: 100%;
      }

      .dashboard-header__meta {
        gap: 0.55rem;
      }

      .dashboard-chip {
        min-width: 0;
        flex: 1 1 30%;
      }

      .workspace-stage {
        min-height: 40rem;
      }

      .workspace-stage__card--primary {
        top: 0.9rem;
        left: 0.9rem;
        width: calc(100% - 1.8rem);
        padding: 0.9rem 0.9rem 0.85rem;
      }

      .workspace-stage__card--primary strong {
        font-size: 1.05rem;
      }

      .workspace-stage__card--primary p {
        font-size: 0.92rem;
        line-height: 1.45;
      }

      .tab-strip__track {
        gap: 0.55rem;
        padding: 0.65rem;
      }

      .tab-pill {
        gap: 0.45rem;
        padding: 0.65rem 0.72rem;
      }

      .tab-pill__icon,
      .tab-pill__close {
        width: 1.28rem;
        height: 1.28rem;
        font-size: 0.66rem;
      }

      .tab-pill__icon {
        width: 0.36rem;
        height: 0.36rem;
      }

      .workspace-stage__panel {
        width: calc(100% - 2rem);
        left: 1rem;
      }

      .workspace-stage__panel--vue {
        top: 17rem;
        right: auto;
      }

      .workspace-stage__panel--react {
        top: 21.5rem;
        right: auto;
      }

      .workspace-stage__panel--angular {
        top: 26rem;
        right: auto;
      }

      .workspace-stage__card--secondary {
        left: 0.9rem;
        right: 0.9rem;
        bottom: 0.5rem;
        padding: 0.6rem;
        gap: 0.4rem;
      }

      .workspace-stage__card--secondary span {
        font-size: 0.72rem;
        padding: 0.42rem 0.58rem;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .tab-strip__track,
      .tab-pill--active,
      .workspace-stage__card--primary,
      .workspace-stage__card--secondary,
      .workspace-stage__panel {
        animation: none;
      }
    }
  </style>
</section>
